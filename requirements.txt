# Core dependencies. Versions are lower bounds, not pins; any TensorFlow 2.12
# or newer release should work. Requires Python 3.9+.
tensorflow>=2.12
numpy>=1.23
pandas>=1.5
scikit-learn>=1.2
matplotlib>=3.6
